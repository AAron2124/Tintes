<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projecto_Final"
        tools:targetApi="31">

        <!-- Login como actividad principal -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity normal, sin launcher -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- Catálogo -->
        <activity android:name=".CatalogoActivity" android:exported="true" />

        <!-- Clientes -->
        <activity android:name=".ClientesActivity" android:exported="true" />
        <activity android:name=".FormularioClienteActivity" android:exported="true" />

    </application>

</manifest>
